{
  // === TypeScript コンパイル設定（ESM + Node.js 22）===
  "compilerOptions": {

    // --- 実行環境（Node.js 22）の対応バージョン ---
    // Node.js 22 は ES2022 を完全サポートしているため最適
    "target": "ES2022",

    // --- モジュール形式（重要：Node.js 22 は ESM を推奨） ---
    // 最新ランタイムでは CommonJS ではなく ES Modules が推奨
    "module": "ES2022",

    // --- モジュール解決ロジック ---
    // "bundler": ESM/URL 参照や最新のパッケージ解決に対応。
    // Node.js 22 の ESM 実行（tsx など）と相性が良い設定。
    "moduleResolution": "bundler",

    // --- 出力ディレクトリ / ルート ---
    // outDir: Lambda にデプロイする dist 配下に JS を出力
    "outDir": "./dist",
    "rootDir": "./src",
    // rootDir: src 配下のみをコンパイル対象にし、出力時に src サブパスを剥がして dist 直下へ配置
    // 例: src/app.ts → dist/app.js, src/handlers/express.ts → dist/handlers/express.js

    // --- import/export をより柔軟に扱えるようにする ---
    "esModuleInterop": true,

    // --- 型安全性を最大化（バグ防止） ---
    "strict": true,

    // --- ソースマップ出力（ローカルデバッグ用） ---
    "sourceMap": true,

     // TypeScript 拡張子（.ts/.tsx）付き import を許可するか。
     // 本プロジェクトは ESM の方針として「拡張子なしの相対 import」を採用するため false。
     // （ランタイムでは .js に解決される）
    "allowImportingTsExtensions": false
  },

  // --- コンパイル対象 ---
  "include": ["src/**/*.ts"]
}
